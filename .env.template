# Anthropic — required. Used by agent.py for all research jobs.
ANTHROPIC_API_KEY=

# Telegram — required. Used by telegram_bot.py for daily brief delivery.
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=

# clippy-api.py /chat endpoint auth. Generate with: openssl rand -hex 32
# Required if anything calls /chat; otherwise the API will reject requests.
AGENT_API_KEY=

# Darwin integration — optional. If both are set, scheduler.py will POST each
# job's findings to Darwin's /ingest endpoint after delivering to Telegram.
# If either is empty, the hook is a no-op (Clippy runs unchanged).
#   DARWIN_INGEST_URL: e.g. http://NintendoPI.local:8769/ingest
#   DARWIN_API_KEY:    copy from Darwin's /home/darwin/darwin/.env
DARWIN_INGEST_URL=
DARWIN_API_KEY=

# CRE Weekly Email Brief — optional. Used by email_sender.py (Mon 10:00 job).
# If any of the three are unset/empty, the email send is skipped (Telegram +
# walnut + Darwin still fire). Send is via Gmail SMTP, multipart HTML+text,
# all recipients BCC'd.
#
#   SMTP_USER:   the Gmail address sending the email (e.g. you@gmail.com)
#   SMTP_PASS:   a Google App Password — NOT the account password.
#                Generate at https://myaccount.google.com/apppasswords
#                Requires 2-Step Verification on the Google account.
#   CRE_EMAIL_RECIPIENTS: comma-separated list of email addresses.
#                e.g. you@gmail.com,colleague@firm.com
SMTP_USER=
SMTP_PASS=
CRE_EMAIL_RECIPIENTS=
